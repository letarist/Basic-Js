let number = +prompt('Ввод для задания 4: Введите число от 0 до 15: ');
function output(a) {
    document.write('Task 4: ');
    while (a <= 15) {
        document.write(' ' + a);
        a++;
    }
}
switch (number) {
    case number:
        output(number);
}
